[pytest]
pythonpath = .
testpaths = tests
markers =
    slow: long-running integration-style checks
filterwarnings =
    ignore:The covariance matrix is not positive definite.*:UserWarning:skfolio\.moments\.covariance\._base
